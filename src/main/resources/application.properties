spring.application.name=cms

# Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/cms_db
spring.datasource.username=root
spring.datasource.password=root

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# Server
server.port=8080

# Email Configuration
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=mcaviit2025@gmail.com
# Use the App Password exactly as provided by Google (spaces are usually okay, but try without if it fails)
spring.mail.password=laicxmjzqbdfgjhl
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true

# Timeouts: Increased to allow enough time for the PDF upload
spring.mail.properties.mail.smtp.connectiontimeout=15000
spring.mail.properties.mail.smtp.timeout=15000

# Logging: THIS IS IMPORTANT. It will show the SMTP error in your console.
spring.mail.properties.mail.debug=true
logging.level.org.springframework.mail=DEBUG